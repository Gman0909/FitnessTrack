services:
  app:
    build: .
    ports:
      - "${PORT:-3001}:3001"
    volumes:
      - fitness_data:/data
    restart: unless-stopped
    environment:
      PORT: "3001"
      DATABASE_PATH: /data/fitness.db

volumes:
  fitness_data:
